# Jekyll configuration for the Cagetti2005tk REMARK-in-progress GitHub Pages site.
# Only the LaTeX write-up and the reproduction artefacts are intended to render
# from gh-pages; everything else is excluded so the source tree does not leak
# into the rendered site.
exclude:
  - .devcontainer*
  - .git*
  - .docker*
  - .cursorignore
  - .cursorindexingignore*
  - .cursorrules
  - .aspell.conf
  - .chktexrc
  - .editorconfig
  - .latexindent.yaml
  - reproduce/
  - Code/
  - Subfiles/
  - Private/
  - legacy/
  - README.md
  - LICENSE
  - Dockerfile
  - pyproject.toml
  - uv.lock
  - .python-version
  - '@local/'
  - '@resources/'
  - '*.tex'
  - '*.bib'
  - '*.bst'
  - '*.cls'
  - '*.sty'
  - '*.bbl'
  - '*.aux'
  - '*.log'
  - tabular/
  - figures/

# Enable relative links
relative_links:
  enabled: true
  collections: true
